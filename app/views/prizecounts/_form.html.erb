<%= form_for(@prizecount) do |f| %>
  <% if @prizecount.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prizecount.errors.count, "error") %> prohibited this prizecount from being saved:</h2>

      <ul>
      <% @prizecount.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :prize_name %><br />
    <%= f.text_field :prize_name %>
  </div>
  <div class="field">
    <%= f.label :prize_id %><br />
    <%= f.text_field :prize_id %>
  </div>
  <div class="field">
    <%= f.label :prize_count %><br />
    <%= f.text_field :prize_count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
